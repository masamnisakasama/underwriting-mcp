# Demo

Run the local mock flow:

```bash
make demo
```

Generate sample PDFs:

```bash
make generate-samples
```

Expected outcomes:

- Case A: `ELIGIBLE_CANDIDATE`
- Case B: `REFER`
- Case C: `NOT_ELIGIBLE_CANDIDATE`

All rules are fictional demo rules.
